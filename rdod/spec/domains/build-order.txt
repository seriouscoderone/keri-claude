Build Order (50 domains, 12 layers):

Layer 0 (foundation — no dependencies):
  accountability/consensus
  accountability/dissemination
  cesr/primitives
  cloud-agent-service/api
  cloud-agent-service/processing
  cloud-agent-service/provisioning
  delegation/authorization
  delegation/lifecycle
  externals/cryptographic-primitives
  externals/persistence
  externals/transport
  privacy/aggregation
  privacy/disclosure
  signify-client/key-management
  signify-client/resources

Layer 1 (depends on layers 0–0):
  delegation/recovery
  identity/anchoring
  identity/key-commitment
  identity/state
  identity/thresholds
  integrity/detection
  privacy/blinding

Layer 2 (depends on layers 0–1):
  cesr/composition
  credential-lifecycle/status
  identity/establishment
  integrity/recovery

Layer 3 (depends on layers 0–2):
  cesr

Layer 4 (depends on layers 0–3):
  delegation
  keri-messaging
  watcher-service/api

Layer 5 (depends on layers 0–4):
  accountability/receipting
  credential-exchange/negotiation
  credential-lifecycle/verification
  integrity/evidence

Layer 6 (depends on layers 0–5):
  accountability
  credential-exchange/proof
  integrity
  witness-service/api

Layer 7 (depends on layers 0–6):
  discovery
  identity

Layer 8 (depends on layers 0–7):
  credential-lifecycle/registry
  witness-service

Layer 9 (depends on layers 0–8):
  credential-exchange
  credential-lifecycle
  privacy
  watcher-service

Layer 10 (depends on layers 0–9):
  cloud-agent-service
  local-agent/api

Layer 11 (services + applications):
  local-agent
  signify-client

