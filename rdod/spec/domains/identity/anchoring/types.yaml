domain_ref: "identity/anchoring"

imports:
  - type: "types://cesr/primitives#Seal"
    usage: "All seal types (DigestSeal, MerkleRootSeal, SourceEventSeal, EventSeal, LastEstSeal, BackerSeal, TypedDigestSeal) are defined in cesr/primitives"
  - type: "types://cesr/primitives#DigestAlgorithm"
    usage: "Digest algorithm enum for seal digest computation"

# Local seal types removed — canonical definitions are in cesr/primitives.
# EventLocationSeal ({i,s,t,p}) removed — not protocol-normative (keripy implementation only).
types: []
