# {{ecosystem_display_name}} -- Credential Catalog

> Industry-standard ACDC credential types for the **{{ecosystem_name}}** ecosystem.
> Generated by `/keri-c0-ecosystem`. Schema fields define the ACDC `a` (attributes) block.

---

## Credential Summary

| ID | Name | Issuer | Holder | Verifiers | Disclosure |
|----|------|--------|--------|-----------|------------|
{{#each credential_catalog}}
| `{{id}}` | {{name}} | {{issuer_role}} | {{holder_role}} | {{verifier_roles}} | {{disclosure_mode}} |
{{/each}}

---

## Credential Details

{{#each credential_catalog}}

### {{name}} (`{{id}}`)

**Description:** {{description}}

**Issuer role:** {{issuer_role}}
**Holder role:** {{holder_role}}
**Verifier roles:** {{verifier_roles}}
**Disclosure mode:** {{disclosure_mode}}

#### Schema Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
{{#each schema_fields}}
| `{{name}}` | {{type}} | {{required}} | {{description}} |
{{/each}}

{{#if chained_from}}
#### Credential Chaining

This credential chains from `{{chained_from}}`. The issuer MUST hold a valid, unrevoked `{{chained_from}}` credential. The edge block (`e`) of this ACDC references the chained credential's SAID, creating a cryptographically verifiable provenance chain.
{{/if}}

#### Privacy Notes

{{#if disclosure_mode == "selective"}}
- Supports selective disclosure via ACDC graduated disclosure. Verifiers receive only the fields they request.
- Holder can prove possession without revealing attribute values (compact disclosure).
{{else if disclosure_mode == "partial"}}
- Uses partial disclosure. Specific field groups are disclosed together per the schema's disclosure rules.
{{else}}
- Full disclosure: all schema fields are revealed to verifiers on presentation.
{{/if}}

---

{{/each}}

## Credential Dependency Graph

Lists chaining relationships between credentials in this ecosystem.

```
{{#each credential_catalog}}
{{#if chained_from}}
{{chained_from}} --> {{id}}
{{/if}}
{{/each}}
```

---

## Next Steps

- Run `/keri-c1-system` to design services that issue, hold, and verify these credentials.
- Credential schemas are anchored in the ecosystem's schema registry once C2 infrastructure is deployed.
